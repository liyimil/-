# frontend

课题 D：前端可视化目录。

## 当前文件

- `index.html`：静态演示页面，展示 D 模块的编排状态、事件看板、规则判定结果和告警流。

## 使用方式

直接用浏览器打开：

```text
frontend/index.html
```

当前页面先使用内置 mock 数据演示。后续如果接后端接口，可以把页面里的 `mockData` 替换成接口返回的 `frontend_data`。
