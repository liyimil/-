# RULE SKILL: 10kV线路保护通道全部退出

## metadata

- skill_id: RULE_0913
- source_rule_id: "913"
- type: event_rule
- event_level: 危急
- event_type: 4-设备运行异常
- valid_time: 10
- sub_type: 综合事件规则
- run_mode: @ULL/
- enabled: true

## source

- source_file: rules.json

## expression

```text
(@1)
```

说明：表达式中的 `@N` 对应 `features.json` 中 `feature_id=@N` 的特征。

## feature_mapping

本规则引用的特征：@1

规则含义按表达式逻辑运算符（`|` 或、`&` 且、`!` 非）解释。

## output_format

```text
$BAY 线路保护通道全部退出
```

## explanation

该规则用于识别「10kV线路保护通道全部退出」事件。

事件类型为「4-设备运行异常」。

事件等级为「危急」。

依赖特征：@1。
