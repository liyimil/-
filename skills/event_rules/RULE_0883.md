# RULE SKILL: [精准]低压电容器开关遥控操作（AVC操作异常）

## metadata

- skill_id: RULE_0883
- source_rule_id: "883"
- type: event_rule
- event_level: 一般
- event_type: 5-操作事件
- valid_time: 10
- sub_type: 综合事件规则
- run_mode: @ULL/
- enabled: true

## source

- source_file: rules.json

## expression

```text
(@1)
```

说明：表达式中的 `@N` 对应 `features.json` 中 `feature_id=@N` 的特征。

## feature_mapping

本规则引用的特征：@1

规则含义按表达式逻辑运算符（`|` 或、`&` 且、`!` 非）解释。

## output_format

```text
$BAY 开关操作（AVC异常）
```

## explanation

该规则用于识别「[精准]低压电容器开关遥控操作（AVC操作异常）」事件。

事件类型为「5-操作事件」。

事件等级为「一般」。

依赖特征：@1。
