# RULE SKILL: 多间隔测控装置告警

## metadata

- skill_id: RULE_0840
- source_rule_id: "840"
- type: event_rule
- event_level: 一般
- event_type: 3-设备异常
- valid_time: 10
- sub_type: 综合事件规则
- run_mode: @ULL/
- enabled: true

## source

- source_file: rules.json

## expression

```text
(@1)
```

说明：表达式中的 `@N` 对应 `features.json` 中 `feature_id=@N` 的特征。

## feature_mapping

本规则引用的特征：@1

规则含义按表达式逻辑运算符（`|` 或、`&` 且、`!` 非）解释。

## output_format

```text
$FAC 测控装置告警（多间隔）
```

## explanation

该规则用于识别「多间隔测控装置告警」事件。

事件类型为「3-设备异常」。

事件等级为「一般」。

依赖特征：@1。
