# RULE SKILL: 压变保护测量二次空开跳开

## metadata

- skill_id: RULE_0834
- source_rule_id: "834"
- type: event_rule
- event_level: 严重
- event_type: 3-设备异常
- valid_time: 10
- sub_type: 综合事件规则
- run_mode: @ULL/
- enabled: true

## source

- source_file: rules.json

## expression

```text
(@1)
```

说明：表达式中的 `@N` 对应 `features.json` 中 `feature_id=@N` 的特征。

## feature_mapping

本规则引用的特征：@1

规则含义按表达式逻辑运算符（`|` 或、`&` 且、`!` 非）解释。

## output_format

```text
$DEV 压变二次空开跳开
```

## explanation

该规则用于识别「压变保护测量二次空开跳开」事件。

事件类型为「3-设备异常」。

事件等级为「严重」。

依赖特征：@1。
