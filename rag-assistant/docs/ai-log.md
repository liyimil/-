# AI 协作决策记录 — RAG 课程助手

> 每条记录包含五个字段。最关键的是「人工判断」——不是"AI说的对"，而是你采纳/修改/拒绝了AI的什么建议、为什么。

---

## 第 1 条：Spec 文件设计与创建

| 字段 | 内容 |
|------|------|
| **目的** | 设计并创建 spec.md，明确项目目标、非目标、验收标准 |
| **输入** | 用户要求基于 course-faq.md 构建 RAG 系统，目标≤5、非目标≥3、验收≥3 |
| **建议** | AI 提出 5 个关键问题（检索方式、LLM 调用、输出格式、拒答策略、多轮对话），提供 spec 框架，分析已有测试覆盖的场景 |
| **人工判断** | 采纳核心框架，修改部分细节：关键词匹配（非向量检索）、mock 服务、纯文本+来源、空时拒答、单轮对话；补充验收标准（拒答回复内容、来源引用格式） |
| **验证** | 创建 spec.md，包含 4 条目标、5 条非目标、7 条验收标准、4 条边界条件（含超长问题处理） |

---

## 第 2 条：超长问题处理规则

| 字段 | 内容 |
|------|------|
| **目的** | 确定超长问题的处理方式 |
| **输入** | 用户询问系统面对超长问题的处理方式 |
| **建议** | AI 分析当前系统无超长问题处理，提出三个方案：截断后检索、直接拒答、不限制 |
| **人工判断** | 采纳"截断到指定长度后检索"方案，指定长度 300 字符（用户修改了 AI 建议的 200 字符） |
| **验证** | 更新 spec.md 边界条件第 4 条 |

---

## 第 3 条：RAG 数据流设计与接口签名

| 字段 | 内容 |
|------|------|
| **目的** | 设计 RAG 六环节数据流，推导 retrieve 和 answer 接口签名 |
| **输入** | 已确定的 spec.md（关键词匹配、Top3、拒答规则） |
| **建议** | AI 提供六环节数据流（资料源→切片→检索→Prompt拼接→生成→输出），推导 retrieve(question, chunks) → Top3 和 answer(question, chunks) → {answer, sources} 接口 |
| **人工判断** | 采纳。数据流清晰，接口职责划分正确（retrieve 负责检索，answer 负责生成+来源引用） |
| **验证** | 用户确认切片策略（按 ## [faq-XX] 切片）和接口签名正确 |

---

## 第 4 条：Design.md 完善与接口设计

| 字段 | 内容 |
|------|------|
| **目的** | 完善 design.md，补充模块接口详细设计和来源引用格式 |
| **输入** | 用户要求 design.md 包含数据流图、模块接口、拒答规则、来源引用 |
| **建议** | AI 设计 retrieve 和 answer 接口的详细说明（输入输出类型、异常处理），确定来源引用格式在回答末尾 |
| **人工判断** | 采纳。接口设计清晰，来源引用格式简洁（回答末尾 + 来源列表） |
| **验证** | 用户确认 design.md 内容完整 |

---

## 第 5 条：不同字数范围的检索策略

| 字段 | 内容 |
|------|------|
| **目的** | 设计不同输入长度下的检索策略 |
| **输入** | 用户要求加入字数从 10 到 1000 的检索策略变化 |
| **建议** | AI 提出三档策略：10-50 字直接检索、50-300 字直接检索、300-1000 字截断到 300 字后检索 |
| **人工判断** | 采纳。三档策略简洁明了，覆盖短/中/长三种输入场景 |
| **验证** | 更新 design.md 第 10 节 |

---

## 第 6 条：开发计划拆解

| 字段 | 内容 |
|------|------|
| **目的** | 根据 spec.md 和 design.md 拆解开发计划 |
| **输入** | 用户要求按测试用例开发、框架搭建、功能开发、集成测试、验收的步骤拆解 |
| **建议** | AI 提出 11 个 task，每个 task 独立可验证，包含验收指标和时间估计 |
| **人工判断** | 采纳。任务粒度合理，每个 task 对应一个可验证的测试用例或里程碑，状态跟踪表简洁实用 |
| **验证** | 创建 docs/tasks.md，包含状态跟踪表和详细任务说明 |

---

## 第 7 条：开发计划调整为"完成一个测试一个"模式

| 字段 | 内容 |
|------|------|
| **目的** | 调整开发计划，确保每个 task 完成后立即验证 |
| **输入** | 用户要求"完成一个测试一个"，中间步骤需要补充验收指标 |
| **建议** | AI 调整状态跟踪表，将"验证方式"列改为"完成即验证"，强调立即验证闭环 |
| **人工判断** | 采纳。每个 task 都是"完成→验证"的闭环，确保开发过程中每一步都可验证 |
| **验证** | 更新 docs/tasks.md 状态跟踪表 |

---

## 第 8 条：T1-T3 框架搭建与测试脚本开发

| 字段 | 内容 |
|------|------|
| **目的** | 完成项目框架搭建、函数骨架、集成测试脚本 |
| **输入** | tasks.md 中 T1-T3 的验收指标 |
| **建议** | AI 创建 retrieve.py、answer.py、main.py 函数骨架，编写 test_integration.py 集成测试脚本（覆盖 10 个测试用例） |
| **人工判断** | 采纳。函数骨架符合 test_basic.py 接口契约要求，集成测试脚本可运行并生成测试报告 |
| **验证** | T1: 目录结构存在；T2: test_basic.py 9/9 通过；T3: 测试脚本运行成功，生成 docs/test-report.md |

---

## 第 9 条：T4-T9 核心功能实现

| 字段 | 内容 |
|------|------|
| **目的** | 实现 RAG 系统核心功能：切片、检索、Prompt拼接、LLM调用、拒答、来源引用、超长问题处理 |
| **输入** | tasks.md 中 T4-T9 的验收指标 |
| **建议** | AI 实现 load_chunks 切片函数（正则分割）、retrieve 检索函数（单字符匹配）、answer 函数（Prompt拼接+LLM调用+来源引用） |
| **人工判断** | 采纳。切片功能正确（10个chunk），检索功能返回Top3，LLM调用成功，拒答规则生效，来源引用正确，超长问题截断处理正确 |
| **验证** | T4: 10个chunk正确切分；T5: Top3返回正确；T6: mock API调用成功；T7: 资料外问题拒答；T8: 回答末尾有来源；T9: 300字符截断；集成测试: 4/10通过 |

---

## 第 10 条：完成未通过测试与最终验收

| 字段 | 内容 |
|------|------|
| **目的** | 读取 rag-assistant 中未完成事项，修复集成测试失败用例，补齐 RAG 交付物 |
| **输入** | docs/handover.md 中记录的未完成问题、tests/questions.json 的 10 个集成测试用例、当前 src/retrieve.py / src/answer.py / src/main.py 实现 |
| **建议** | AI 先复现测试失败，再建议把检索从泛化 n-gram 匹配改为课程领域关键词、意图扩展和标题加权；对明确资料外/混淆问题增加拒答规则；让 CLI 真正加载 course-faq.md；补充 test-record.md 和 reflection.md |
| **人工判断** | 采纳检索规则优化和 CLI 加载修复，拒绝通过修改测试用例来“凑通过”；保留 mock LLM 调用接口，但最终回答以检索到的 FAQ chunk 为准，确保来源可复核；未创建 bug-fix-lab，因为它不属于本 RAG 目录范围 |
| **验证** | python tests/test_basic.py：9 通过 0 失败；python tests/test_integration.py：10 通过 0 失败；CLI 冒烟测试资料内问题能返回来源、资料外问题能拒答；check-submission.sh 中 RAG 文件和代码检查通过，剩余 BLOCKED 仅来自仓库级 bug-fix-lab 缺失 |

---

## 第 11 条：补强 test-record.md 的可复核性

| 字段 | 内容 |
|------|------|
| **目的** | 让助教不用运行代码，也能通过测试记录判断结果是否正确 |
| **输入** | 用户明确要求 test-record.md 必须包含“测试的输入 / 预期 / 实际”，并且“不用跑代码就能验证结果是否正确” |
| **建议** | AI 建议把原来的概要式测试记录改为逐条表格，覆盖 10 个集成测试用例，并额外记录 CLI 手动验证的输入、预期和实际输出 |
| **人工判断** | 采纳。原记录只写了命令和通过数，证据不够细；改成表格后，每个测试用例都有输入、预期关键词或拒答规则、实际来源或实际输出，更符合“可复核交付” |
| **验证** | 重写 docs/test-record.md，包含自动化测试结果表、10 条集成测试明细、资料内/资料外 CLI 手动验证和最终结论 |

---

## 第 12 条：重写 README.md 的复现路径

| 字段 | 内容 |
|------|------|
| **目的** | 让同桌或助教能按 README 独立复现运行结果 |
| **输入** | 用户要求 README.md 必须包含安装、运行、测试命令，复现结果 |
| **建议** | AI 建议去掉旧脚手架语气，保留项目目标和目录结构，重点补充环境要求、安装方式、运行命令、测试命令、Windows 编码提示和可复现检查顺序 |
| **人工判断** | 采纳。README 的读者不是开发中的自己，而是验收者；因此命令要直接可复制，预期输出要写清楚，避免只写“快速开始”但没有判断标准 |
| **验证** | 重写 README.md；按 README 中的命令运行 test_basic.py、test_integration.py、资料内 CLI、资料外 CLI，结果分别为 9/9、10/10、资料内带来源、资料外拒答 |

---

## 第 13 条：完善 reflection.md 的思考证据

| 字段 | 内容 |
|------|------|
| **目的** | 用复盘证明自己理解了问题和取舍，而不只是让 AI 执行修改 |
| **输入** | 用户要求 reflection.md 包含“认知变化 + 困难 + 改进”，证明有思考 |
| **建议** | AI 建议把复盘从简短总结扩展为第一人称反思：说明对 RAG 的认知从“扩大召回”变成“控制证据”，记录中文检索噪声、跨段落召回、CLI 与测试路径不一致等困难，并写出没有修改测试、没有引入向量库的取舍 |
| **人工判断** | 采纳。复盘不能只写“测试通过”，而要说明为什么这样修、为什么不走更复杂方案；这能体现项目边界意识和人工判断 |
| **验证** | 重写 docs/reflection.md，包含认知变化、遇到的困难、判断与取舍、改进结果、最终验证和后续改进方向 |
