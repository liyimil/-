# AI 协作决策记录 — task-manager Bug 修复

> 每条记录包含五个字段。最关键的是「人工判断」——不是"AI说的对"，而是你采纳/修改/拒绝了AI的什么建议、为什么。

---

## 第 1 条：Bug 定位与根因分析

| 字段 | 内容 |
|------|------|
| **目的** | 请 AI 分析 test.py 测试失败的原因，定位 task_manager.py 中的 Bug |
| **输入** | test.py 的失败输出（3个测试失败）和 task_manager.py 源码 |
| **建议** | AI 指出两个 Bug：1) priority 未做 int() 转换 2) title.strip() 后未检查空字符串 |
| **人工判断** | 采纳。AI 的定位准确，与测试失败信息吻合：Bug1 对应测试3，Bug2 对应测试2 |
| **验证** | 手动检查代码第 21 行和第 23 行，确认缺少类型转换和空值校验 |

---

## 第 2 条：Bug1 修复方案选择

| 字段 | 内容 |
|------|------|
| **目的** | 请 AI 提供 Bug1（字符串 priority）的修复方案 |
| **输入** | Bug 根因：priority 直接存入字典未转换；测试期望 'high'/'low' 能成功转为整数 |
| **建议** | AI 提供两个方案：1) 直接 int() 转换（会抛错）2) 添加 PRIORITY_MAP 映射表 |
| **人工判断** | 采纳方案2。测试期望 'high'/'low' 能成功转换而非抛错，所以需要映射表 |
| **验证** | 运行测试，确认 `add_task(tasks, '重要任务', 'high')` 不再抛异常，priority 变为 10 |

---

## 第 3 条：Bug2 修复方案确认

| 字段 | 内容 |
|------|------|
| **目的** | 请 AI 提供 Bug2（空 title）的修复方案 |
| **输入** | Bug 根因：title.strip() 后未检查空字符串；测试期望抛出 ValueError |
| **建议** | AI 建议在 strip 后添加 `if not trimmed_title: raise ValueError("title 不能为空")` |
| **人工判断** | 采纳。方案简洁，错误信息包含 'title' 关键词，满足测试断言要求 |
| **验证** | 运行测试，确认空字符串和纯空格输入都抛出 ValueError |

---

## 第 4 条：最小修复约束

| 字段 | 内容 |
|------|------|
| **目的** | 确保修复符合"每个 Bug ≤ 5 行"的最小修复原则 |
| **输入** | 两个 Bug 的修复代码行数 |
| **建议** | AI 确认 Bug1 修复 4 行（映射表 + 转换逻辑），Bug2 修复 2 行（校验 + 抛错） |
| **人工判断** | 采纳。总计 6 行代码，符合最小修复要求，未引入多余改动 |
| **验证** | 查看 git diff，确认只新增必要代码，未修改无关逻辑 |

---

## 第 5 条：回归测试验证

| 字段 | 内容 |
|------|------|
| **目的** | 验证修复后所有测试通过，未引入新 Bug |
| **输入** | 修复后的 task_manager.py |
| **建议** | AI 建议运行 `python3 test.py` 确认全部 7 个测试通过 |
| **人工判断** | 采纳。测试输出 `7 通过, 0 失败`，回归测试完整通过 |
| **验证** | `python3 test.py` 输出 `✅ 所有测试通过` |
